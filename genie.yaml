coffee:
  targets:
    - node

yaml:
  targets:
    node:
      - preset: js
        glob:
          - "{src,test}/**/*.yaml"
      - preset: json
        glob:
          - "src/**/*.schema.yaml"

pug:
  targets:
    node:
      - preset: render
        glob:
          - "email/**/*.pug"

schema:
  glob: "src/**/*.schema.yaml"
    
sky:
  namespace: dashkite
  ses:
    templates:
      - name: authenticate
        subject: Authenticate with DashKite

  graphene:
    - name: enchant-test
      db: 32ro5fmo6qlrb7frp0k3fk1gt
      collections:
        - byname: bundles